# Server configuration for check-servers.sh
# Format: IP DISPLAY_NAME
# Lines starting with # are ignored.

# Ping timeout in seconds (e.g., 0.5 for 500ms).
timeout = 0.2
retries = 3

[local]
10.0.0.1 router

[remote]
9.9.9.9 quad9